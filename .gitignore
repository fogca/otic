node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
# vite-plugin-pwa's dev-mode service worker (devOptions.enabled in
# vite.config.ts) -- regenerated on every `vite dev`, not build output
# meant to ship.
/dev-dist

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local-only (not pushed to git)
CMS/
